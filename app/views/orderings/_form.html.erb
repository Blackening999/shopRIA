<%= form_for(@ordering) do |f| %>
  <% if @ordering.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ordering.errors.count, "error") %> prohibited this ordering from being saved:</h2>

      <ul>
      <% @ordering.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
